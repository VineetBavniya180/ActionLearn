name: Create Issue Comment

on:
  issue_comment:
    types: [created]

  jobs:
    createComment:
      runs-on: ubuntu-laatest 
      steps:
        - name: "dump git event"
          run: echo "${{ toJson(github.event) }} | jq ."
          shell: bash
name: Create Issue Comment

on:
  issues:
    types: [created, edited, deleted, opened]

jobs:
  createComment:
    runs-on: ubuntu-latest 
    steps:
      - name: "dump git event"
        run: echo "${{ toJson(github.event) }} | jq ."
        shell: bash